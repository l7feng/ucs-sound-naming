# Skill 矛盾检查报告

- **检查日期**：2026-08-20
- **检查对象**：UCS skill（master）与项目文档（`D:\Ai_Project\UCS\规则更新\`）的一致性
- **检查范围**：SKILL.md、install.md、首次使用指南.md、ucs-config.default.json、references/，以及项目内《UCS_命名流程规范》《多Agent协作方案》《Skill跨设备部署方案》《known-conflicts》
- **结论**：本次项目变更绝大部分已同步进 skill，无冲突；需修复以下 6 处文档内部不一致 + 1 项发布待办。

---

## 🔴 实质矛盾（必须修复）

### #1 附带文件扩展名清单四处不一

| 位置 | 当前内容 |
|---|---|
| `SKILL.md`（工作流） | `.txt .pdf .jpg .png .html .htm README` |
| `UCS_命名流程规范.md`（v1.4） | `.txt .pdf .jpg .png .html .htm README` |
| `ucs-config.default.json` | 上述基础上多 `.md` |
| `Skill跨设备部署与用户习惯适配方案.md` | 缺 `.htm` |

- **问题**：同一规则四处表述不一致，执行时可能按不同清单处理。
- **建议**：统一为 `.txt .pdf .jpg .png .html .htm .md README`（补全 `.md` 与 `.htm`）。

### #2 install.md 对照表放置表述与双轨制冲突

- **位置**：`install.md:67`
- **当前表述**：「对照表与矛盾裁决放在 `<UCS_ROOT>/规则更新/`（或 skill references 内）」
- **问题**：与双轨制冲突——
  - 对照表（`ucs-zh-en.tsv`）是 skill 自带文件，**不落项目**；
  - 矛盾裁决（`known-conflicts.md`）编辑源在项目 `规则更新\`，发布副本在 skill `references\`。
- **建议**：改为「对照表 skill 自带；矛盾裁决编辑源在项目 `规则更新\`，发布副本同步至 skill `references\`」。

---

## 🟡 过期表述（建议修复）

### #3 跨设备方案状态仍写「待执行」

- **位置**：`Skill跨设备部署与用户习惯适配方案.md`
- **问题**：方案中的 #2-#6 项（路径零假设、配置化、双轨制、按需生成等）实际均已落地，文档状态未更新。
- **建议**：将已执行项标记为「已完成」，仅保留未做项。

### #4 多Agent方案仍提及「脚本」

- **位置**：`多Agent协作_Skill文件输出管理方案.md`
- **问题**：`gen-creatures-list.ps1` / `gen-creatures-ck-zh.ps1` 已删除，方案仍引用脚本机制。
- **建议**：删除/替换脚本相关表述，改为按需生成流程。

### #5 SKILL.md 顶部描述过时

- **位置**：`SKILL.md:8`
- **当前表述**：「输出是可复制、可下载的 .txt 命名列表」
- **问题**：与"清单按需生成"决策不符（默认展示双列映射，仅在用户需要时生成 .txt）。
- **建议**：改为「展示中英文双列映射，按需生成 .txt 清单」。

---

## ⚪ 信息缺失 / 待办

### #6 首次使用指南缺两项说明

- **位置**：`首次使用指南.md`
- **缺失**：① 库自带说明文件（`.txt/.pdf/.jpg/.png/.html/.htm/.md/README`）会被删除；② 命名清单按需生成，默认不输出 .txt。
- **建议**：补入两条说明，避免用户首次使用困惑。

### #7 zip 落后 master（发布待办）

- **位置**：`D:\Ai_Project\Skills\ucs-sound-naming.zip`
- **问题**：11:21 SKILL.md / config 更新后未重打包；当前 zip 内容落后 master。
- **建议**：本次不打包（按需打包原则）；下次发布/传播前必须重打包。

---

## 修复执行清单

1. 统一扩展名清单 → 4 处（SKILL.md、规范v1.4、config、跨设备方案）
2. 修正 install.md:67 对照表表述
3. 更新跨设备方案状态（#3）
4. 清理多Agent方案脚本表述（#4）
5. 更新 SKILL.md:8 描述（#5）
6. 补首次使用指南两项说明（#6）
7. 修复后 master ↔ 副本同步；zip 按需打包（#7）

> 说明：本报告与 `known-conflicts.md` 分开保存。`known-conflicts.md` 记录 UCS 命名规则/数据层面的矛盾裁决（长期口径依据）；本报告记录 skill 文档自身的工程问题（修复后即归档）。
